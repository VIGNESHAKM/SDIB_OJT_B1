// calculagte the area of the rectenguler using aairthmetic operator s
#include<stdio.h>
int main ()
{
     int area , length, width;
     printf("enter the length   and width");
     scanf("%d %d", &length , &width);
     area = length* width ;
     printf("arra of the   rec isd the %d", area);
     return 0;
   
}
