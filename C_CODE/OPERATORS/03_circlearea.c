// calculagte the area of the circle using aairthmetic operator
#include<stdio.h>
int main ()
{
     float area , redious;
     printf("enter the length   and width");
     scanf("%f", &redious);
     area = (3.14 * redious * redious);
     printf("arra of the circule is the %f", area);
     return 0;
   
}
