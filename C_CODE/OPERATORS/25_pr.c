#include <stdio.h>
void main()
{   
	int x=(20 || 40 ) && (10);
	printf("x= %d",x);
}   
 // in frist 20  or  with 40 both are non zero value so written 1
 // again 1  and with 10 agzain both are non zeri value  sp written 1 spo ans  is 1 