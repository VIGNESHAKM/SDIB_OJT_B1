#include<stdio.h>
struct arith;
{
    int a,b,result;
}
int main()
{
    struct a with a;
    scanf("%d %d &A,a,&A;");
    add(A);
    printf("%d A.result");
    add(struct arith A);
    {
        A.result=A.a+A.b;
        printf("%d",A.result);
        }

    }

