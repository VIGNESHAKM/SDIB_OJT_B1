#include<stdio.h>
struct arith;
{
    int a,b;
    int(*fnptr)(int,int)
}
printf("%d",add(a,b));
int addition (int x,int y)
{
    return x+y;
    int main()
    {
        struct arith add,sub;
        printf("arith value for a&b");
        scanf("% %d",add a,&add b);
        sub=add;
        add.fnptr=addition;
        sub.fnptr=substraction;
        printf("%d",addition ptr are add pntr(add.a,add.b));
        }


