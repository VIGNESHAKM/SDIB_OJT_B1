#include<stdio.h>
int main(int argc,char*argv[])
{
    int a,b,result;
    int i;
    sscanf(argv[i]"%d",&a);
    sscanf(argv[i]"%d",&b);
    if(stramp(argv[3],"A",==0)
    {
        result=a+b;
        printf("the sum is %d ",result);
    }
    elseif(tramp(argv[3],"s")==0)
}
result=a-b;
printf("the dispaly is %d",result)
;
}
elseif(stramp(argv))    }



}